HEADLESS=true
CLAIM_ATTEMPTS=2
PAUSE_MIN_SEC=5
PAUSE_MAX_SEC=20
RETRY_PER_ATTEMPT=2
PROXY_ROTATION=roundrobin
USE_FREE_PROXIES=true
FREE_PROXY_VALIDATE=false
FREE_PROXY_TIMEOUT=5

# === fitur auto-24h ===
STAY_RUNNING=true           # true = daemon mode (loop), false = sekali scan & selesai
COUNTDOWN_STEP_SEC=60       # interval update countdown (detik)
PER_WALLET_DELAY_SEC=2      # jeda pendek antar wallet ketika looping
